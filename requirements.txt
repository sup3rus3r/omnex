# ── Core framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# ── Ingestion ─────────────────────────────────────────────────────────────────
python-magic>=0.4.27          # File type detection via libmagic (not extension)
watchdog>=4.0.0               # File system monitoring for incremental indexing
pypdf2>=3.0.0                 # PDF text extraction
python-docx>=1.1.0            # DOCX extraction
openpyxl>=3.1.0               # XLSX extraction
python-pptx>=0.6.23           # PPTX extraction
beautifulsoup4>=4.12.0        # HTML extraction
lxml>=5.2.0                   # HTML/XML parser

# ── ML — Embeddings & Models ──────────────────────────────────────────────────
torch==2.5.1                  # PyTorch — pin to cu124 build, do not auto-upgrade (2.11.0 is broken)
torchvision>=0.18.0,<0.21.0
transformers>=4.41.0          # CLIP, CodeBERT, general HuggingFace models
sentence-transformers>=3.0.0  # MiniLM text embeddings
openai-whisper>=20231117      # Audio/video transcription
insightface>=0.7.3            # ArcFace detection + 512-dim identity embeddings (ONNX, no TF)
onnxruntime-gpu>=1.19.0       # ONNX GPU runtime for InsightFace

# ── Vector Index ──────────────────────────────────────────────────────────────
usearch>=2.9.0                # Compressed file-based vector index, i8 quantization (~4x vs float32)

# ── Storage ───────────────────────────────────────────────────────────────────
pymongo>=4.7.0                # MongoDB client

# ── Media Processing ──────────────────────────────────────────────────────────
pillow>=10.3.0                # Image processing + thumbnail generation
opencv-python>=4.9.0          # Video frame extraction
ffmpeg-python>=0.2.0          # Audio/video processing

# ── ML Utilities ─────────────────────────────────────────────────────────────
scikit-learn>=1.5.0           # DBSCAN face clustering
numpy>=1.26.0
tqdm>=4.66.0                  # Progress bars for ingestion
huggingface-hub>=0.23.0       # Model downloads

# ── API Utilities ─────────────────────────────────────────────────────────────
httpx>=0.27.0                 # Async HTTP client (Ollama calls)
python-multipart>=0.0.9       # File upload support
aiofiles>=23.2.0              # Async file I/O

# ── Hashing & Utilities ───────────────────────────────────────────────────────
xxhash>=3.4.0                 # Fast content hashing for deduplication

# ── Remote Access ─────────────────────────────────────────────────────────────
pyngrok>=7.0.0                # Auto ngrok tunnel for remote access
